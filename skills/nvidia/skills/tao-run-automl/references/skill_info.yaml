name: tao-run-automl
type: workflow
prerequisites:
  required:
  - name: model
    description: A TAO network skill with selected-action defaults and AutoML/HPO notes.
  - name: action_inputs
    description: Dataset, checkpoint, teacher, calibration, or artifact inputs required by the selected action.
  - name: platform
    description: Compute backend available through the TAO SDK, usually slurm, kubernetes, docker, or brev.
  - name: skill_bank_path
    description: Path to this skill bank so AutoMLRunner can load model skills and selected-action specs.
  optional:
  - name: eval_dataset_uri
    description: Validation or evaluation dataset URI when required by the selected model skill.
  - name: algorithm
    description: AutoML search algorithm such as bayesian, hyperband, asha, bohb, llm, hybrid, or autoresearch.
  - name: llm_endpoint
    description: Required for LLM-powered AutoML algorithms; alias base_url is accepted.
  - name: llm_model
    description: Required for LLM-powered AutoML algorithms; alias model is accepted.
  - name: llm_api_key
    description: Required for LLM-powered AutoML algorithms if not already available in the environment; alias api_key is accepted and must never be printed.
actions:
  run:
    description: Create a fresh timestamped AutoML runner script and launch TAO HPO through AutoMLRunner.
  status:
    description: Inspect an existing AutoML workflow folder, runner log, PID, or SDK state for progress.
  resume:
    description: Resume or recover an explicitly requested existing AutoML experiment.
depends_on:
  models:
  - <network>
  platform:
  - tao-sdk
  - brev
description: TAO AutoML/HPO workflow for launching model/action-agnostic hyperparameter optimization through AutoMLRunner while grounding
  model- and action-specific details in the selected model skill.
