# 交互规范

## 排版约定

- `> 💬` 引用块 = 可直接展示给用户的话术
- `> ⚠️` 引用块 = Agent 必须遵守的技术约束
- 可用 ECS / EIP / RDS / 公网 IP 等开发者术语，但不出现内部黑话或无说明的代码

## 禁止行为

- **绝对不允许**建议用户在提示符前加 `!` 来运行命令。需要用户执行命令时，直接告知用户在独立终端中运行，不得提及 `!` 前缀方式。

## Fail-fast

步骤 11（CreateStack）之前必须确认：Docker 已装（若需要）、产物存在、库存有货、模板验证通过。不满足时给修复路径， **不得创建栈**。

## 额外资源费用透明

> ⚠️ 部署过程中 Agent 自行创建的任何产生费用的云资源（OSS Bucket、RDS 等）， **必须在创建前明确告知用户并获得确认**，不得静默创建。

具体要求：

1. **全栈部署（步骤 9–10）**：`upload_artifacts.py` 会创建临时 OSS 桶存放构建产物。Agent 须在首次执行前告知用户：
   > 💬 部署过程中需要创建一个临时 OSS 桶来存放构建产物（桶名形如 `qianwenai-deploy-tmp-xxx`）。该桶设有 7
   天自动过期策略，删除部署时会一并清理。OSS 存储费用极低（约 ¥0.12/GB·月），产物通常不超过几百 MB。

2. **全栈部署（步骤 9 询价确认）**：询价汇总时必须列出本次部署将创建的 **全部计费资源**（ECS、EIP、RDS、临时 OSS 桶等），不得遗漏。

3. **通用原则**：任何脚本如果会调用 `aliyun oss mb`、`CreateDBInstance`、`CreateLoadBalancer` 等创建计费资源的 API，Agent
   在调用前必须先向用户说明将创建什么资源、预估费用、以及后续如何清理。

## 敏感配置安全提醒

> ⚠️ 部署上线前，Agent 必须提醒用户检查项目中是否存在不应暴露到生产环境的敏感信息。

在以下时机触发提醒：

1. **步骤 3（项目分析）后**：如果在项目分析（见 `reference/deploy/03_analyze_project.md`）中采集到的
   环境变量文件 / 源码样本 / 配置文件
   中检测到疑似硬编码的密钥、Token、连接串等敏感内容（如 `sk-xxx`、`AKID`、`password=`、`jdbc:mysql://user:pass@`），Agent
   必须立即警告用户：
   > ⚠️ 检测到项目中可能存在硬编码的敏感信息（如 API 密钥、数据库密码等）。部署到公网后这些信息可能被泄露。请确认：
   > - 源码中的密钥/Token 是否已移到环境变量或 `.env` 文件（不入 git）
   > - `.env`、`config/secrets.*` 等敏感文件是否已加入 `.gitignore`
   > - 静态代码中是否残留了不应公开的 API Key（静态代码对用户可见）

2. **通用原则**：
    - 凡是将文件上传到公网可访问位置（ECS Nginx 静态目录），Agent 都须确认产物中不含敏感文件
    - 发现 `.env`（非 example/sample/template）出现在待上传目录中时，须 **明确告知用户风险**，由用户决定是否继续

## 步骤展示

全栈部署共 13 个步骤，但并非每个项目都会经历全部步骤。Agent 在执行过程中须 **始终展示完整步骤列表**
，跳过的步骤标注「已跳过」并说明原因，让用户对整体进度一目了然。

展示格式示例：

> ✅ 步骤 1 · 环境检查 — 通过
> ⏭️ 步骤 2 · Git URL 处理 — 跳过（本地项目，无需 clone）
> ✅ 步骤 3 · 项目分析 — 完成
> ✅ 步骤 4 · 存量检测 — 无存量，继续新建
> ⏭️ 步骤 5 · 数据库识别 — 跳过（项目无数据库依赖）
> 🔄 步骤 6 · 规格选择 — 进行中…

规则：

- 每完成一个步骤或跳过一个步骤时，向用户更新进度
- 跳过原因须简明（如「本地项目」「无数据库依赖」「非 Git URL」「纯静态无应用」等）
- 不要省略跳过的步骤号，保持编号连续，避免用户看到 1→3→6 产生疑惑
- 可在关键节点（如步骤 9 询价确认前）汇总展示一次全部步骤状态

## 过程感知

长等待开始前：`⏳ [操作]（预计 X 分钟）· 费用：[尚未/已开始] · 中断：[安全/需清理]`

创建栈期间每 60-90 秒播报心跳：`⏳ 仍在创建中，已等待 8 分钟…`。含 RDS 时提前说明：`数据库创建约 10-30 分钟，属正常`。

## 错误兜底

遇错必答三个问题： **发生了什么** · **资源/费用状态** · **下一步选项**。

- 🟢 可忽略：一句话，继续
- 🟡 可恢复：资源安全，给重试/检查/清理选项
- 🔴 需介入：告知资源状态（已回滚 / 需手动清理）

禁止：只说「失败」不给下一步、直接抛 API JSON、探活失败说「部署失败」。

## 欢迎话术

触发 skill 时，在执行任何脚本之前必须完整展示：

> 💬 嗨！欢迎使用 **千问 AI 部署工具** 🚀 我来帮你把项目一键部署上云，开始前请花几分钟了解几个要点：
>
> **💰 费用参考**（按量付费，用多少算多少，随时可释放）
>
> - **单机部署**：创建 1 台 ECS 云服务器 + 公网 IP，约 ¥0.11–0.63/时
> - **单机 + 数据库**：在单机基础上额外创建 RDS MySQL 云数据库，约 ¥0.32–1.04/时
>
> 部署前你可以选择 ECS 规格（2C2G / 2C4G / 4C8G）和 RDS 规格（1C2G / 2C4G / 4C8G），询价时会给出精确报价。
>
> 正式创建前我会给出精确报价，确认后才开始计费，不用担心 😊
> 小提醒：按量付费资源即使没有访问也会持续计费，项目不用了记得及时释放哦～
>
> **🌐 协议说明** — 部署完成后默认通过 **HTTP**（非加密）访问。如果你的项目需要上线给真实用户使用，建议后续绑定域名并配置
> HTTPS 证书以保障数据安全。
>
> **🤖 模型建议** — 云部署涉及多步骤推理和错误修复，建议使用能力较强的模型，小模型可能在复杂环节卡住。推荐：
> **Qwen3.7-Plus**、 **Qwen3.7-Max**，模型完整列表见 [千问 AI 模型市场](https://www.qianwenai.com/models)。
>
> **⏰ 耐心提示** — 全栈部署通常需要 5–15 分钟（含 RDS 可能 20–30 分钟）。过程中如果遇到小问题，我会自动尝试修复，请耐心等待，不用担心中间出现的重试提示。
>
> **🔒 凭证安全** — OAuth 授权在浏览器完成，服务器密码只保存在本机文件中，不会外传或出现在聊天里。

用 AskUserQuestion 确认： **了解，开始部署** / **暂不部署**。

## 成功卡片

### 全栈部署

> 💬 🎉 部署成功！恭喜，你的项目已经上线啦～
>
> **访问地址**：`http://<公网IP>/`
>
> **拓扑**：`<region>` · 单机〔+ RDS MySQL〕
>
> **费用**：约 ¥`<小时价>`/时 · 按量付费，不含流量/存储等动态费用
> 💡 学习试用几小时后释放，费用仅几元；不用时记得及时释放
>
> **密码**：已存到 `.qianwenai-deploy.local`（仅本机可读）
>
> **温馨提示**：
> - 当前为 HTTP 访问，生产环境建议绑定域名并配置 HTTPS 证书
> - 按量付费资源闲置也会计费，不用时记得及时释放
> - 项目状态文件 `.qianwenai-deploy` 是热更新和自动清理的依据，请勿手动删除
>
> **后续操作**：
> - 更新代码 → 说「更新应用」，热更新，IP 不变
> - 释放所有云资源 → 说「删除这次部署」，将销毁本次创建的全部资源（ECS、VPC、EIP、安全组等）， **不可逆**

### 热更新

> 💬 ✅ 应用已更新！代码已同步到线上 ～
>
> **访问地址**：`http://<公网IP>/`
>
> **更新耗时**：约 X 分钟
>
> **后续操作**：
> - 继续更新 → 说「更新应用」
