ROSTemplateFormatVersion: '2015-09-01'
Description: 'qianwenai single-host deployment: 1 ECS + EIP + VPC + SecurityGroup. Static hosting + app service co-located on one VM.'

Parameters:
  AppName:
    Type: String
    Description: 应用名（用于资源命名），仅小写字母数字和短横线
    Default: app
    AllowedPattern: '^[a-z][a-z0-9-]{0,30}$'
  InstanceType:
    Type: String
    Description: ECS 实例规格
    Default: ecs.e-c1m2.large
  ImageId:
    Type: String
    Default: aliyun_3_x64_20G_alibase_20240528.vhd
    Description: 操作系统镜像。默认 Alibaba Cloud Linux 3 x86_64。
  Password:
    Type: String
    NoEcho: true
    Description: ECS 登录密码（8-30 位，含大小写字母+数字）
    MinLength: 8
    MaxLength: 30
  SystemDiskSize:
    Type: Number
    Default: 40
    MinValue: 20
    MaxValue: 500
  ZoneId:
    Type: String
    Description: 可用区 ID（选取库存检查中有货的可用区，见 reference/deploy/08_check_stock.md）
  AppPort:
    Type: Number
    Default: 8080
    Description: 应用服务监听端口（被 Nginx 反代到 /api/）
  UserDataScript:
    Type: String
    Description: bootstrap 脚本（明文，ROS 会自动 base64 编码）
    Default: |
      #!/bin/bash
      echo "no userdata provided" > /var/log/qianwenai-bootstrap.log

Resources:
  Vpc:
    Type: ALIYUN::ECS::VPC
    Properties:
      VpcName:
        Fn::Sub: qianwenai-${AppName}-vpc
      CidrBlock: 10.0.0.0/16
      Tags:
        - Key: from
          Value: qianwenai
        - Key: qianwenai-appName
          Value:
            Ref: AppName

  VSwitch:
    Type: ALIYUN::ECS::VSwitch
    Properties:
      VpcId:
        Ref: Vpc
      ZoneId:
        Ref: ZoneId
      CidrBlock: 10.0.1.0/24
      VSwitchName:
        Fn::Sub: qianwenai-${AppName}-vsw
      Tags:
        - Key: from
          Value: qianwenai

  SecurityGroup:
    Type: ALIYUN::ECS::SecurityGroup
    Properties:
      SecurityGroupName:
        Fn::Sub: qianwenai-${AppName}-sg
      VpcId:
        Ref: Vpc
      SecurityGroupIngress:
        - PortRange: 80/80
          Priority: 1
          IpProtocol: tcp
          SourceCidrIp: 0.0.0.0/0
          NicType: intranet
        - PortRange: 443/443
          Priority: 1
          IpProtocol: tcp
          SourceCidrIp: 0.0.0.0/0
          NicType: intranet
      Tags:
        - Key: from
          Value: qianwenai

  EcsInstance:
    Type: ALIYUN::ECS::InstanceGroup
    Properties:
      ImageId:
        Ref: ImageId
      InstanceType:
        Ref: InstanceType
      InstanceChargeType: PostPaid
      Password:
        Ref: Password
      SecurityGroupId:
        Ref: SecurityGroup
      VSwitchId:
        Ref: VSwitch
      VpcId:
        Ref: Vpc
      InstanceName:
        Fn::Sub: qianwenai-${AppName}-ecs-[1,2]
      HostName:
        Fn::Sub: qianwenai-${AppName}-ecs-[1,2]
      MaxAmount: 1
      MinAmount: 1
      SystemDiskCategory: cloud_essd
      SystemDiskSize:
        Ref: SystemDiskSize
      AllocatePublicIP: false
      UserData:
        Ref: UserDataScript
      Tags:
        - Key: from
          Value: qianwenai
        - Key: qianwenai-appName
          Value:
            Ref: AppName

  Eip:
    Type: ALIYUN::VPC::EIP
    Properties:
      Name:
        Fn::Sub: qianwenai-${AppName}-eip
      InternetChargeType: PayByTraffic
      Bandwidth: 5
      Tags:
        - Key: from
          Value: qianwenai

  EipAssoc:
    Type: ALIYUN::VPC::EIPAssociation
    Properties:
      AllocationId:
        Ref: Eip
      InstanceId:
        Fn::Select:
          - 0
          - Fn::GetAtt:
              - EcsInstance
              - InstanceIds

Outputs:
  PublicIp:
    Description: 公网访问 IP（静态 http://<IP>/，应用 http://<IP>/api/）
    Value:
      Fn::GetAtt:
        - Eip
        - EipAddress
  EcsInstanceIds:
    Description: ECS 实例 ID 列表
    Value:
      Fn::GetAtt:
        - EcsInstance
        - InstanceIds
  EcsPrivateIps:
    Description: ECS 内网 IP 列表
    Value:
      Fn::GetAtt:
        - EcsInstance
        - PrivateIps
  StackName:
    Value:
      Ref: ALIYUN::StackName
