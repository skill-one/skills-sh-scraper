# compute · 计算

Use for ECS and Linux diagnosis, ECS deployment, ACK clusters, and E-HPC jobs.

| Theme | Core queries | High-signal anchor queries |
| --- | --- | --- |
| ECS diagnosis | ECS、故障、SSH、卡顿、磁盘 | diagnosis |
| Linux diagnosis | Linux、巡检 | SysOM, inspection |
| System resources | CPU、内存、IO | resource |
| Deployment | 部署 | ECS, AppManager, deploy |
| Compute jobs | 作业、计算任务 | E-HPC, job |
| Containers | 容器、集群 | ACK, Kubernetes, container |

Keep ECS/Linux troubleshooting under `compute` because that is the current platform assignment. Use `operations` for cross-product inventory, cloud governance, QianWen authentication, or CLI guidance.
