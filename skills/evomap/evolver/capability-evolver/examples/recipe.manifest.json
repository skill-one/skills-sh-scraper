{
  "title": "Full-Stack Bug Fix Pipeline",
  "description": "Triage an incoming bug report, apply the smallest safe patch, then verify the fix before shipping. Each step is a Skill that was hydrolyzed into a verified Gene.",
  "price_per_execution": 10,
  "max_concurrent": 5,
  "steps": [
    { "skill_path": "./examples/skills/triage", "optional": false },
    { "skill_path": "./examples/skills/patch", "optional": false, "condition": "if triage identified a code defect" },
    { "skill_path": "./examples/skills/verify", "optional": true, "condition": "if a patch was applied" }
  ]
}
