## Phase 5.6: Issue gate — are there Printing Press improvements?

Apply the router issue-gate boundary. Each WU becomes one flat top-level
issue (no parent, no sub-issue hierarchy).

**Skip filing when** the Do table is empty, every survivor is Skip, or every
survivor is printed-CLI-specific. Still write the local retro (manuscript
proofs plus `/tmp/printing-press/retro/`), present findings, then jump to
Phase 6 Step 6 (local-only paths).

Otherwise continue to packaging and confirmation.

Next: phases/09-package-upload-present.md
