---
title: dg api run get
triggers:
  - "details about a specific run"
---

```bash
dg api run get <RUN_ID>
```

## MCP equivalent

`get_run` — prefer it when the Dagster Plus MCP server is connected.

Params: `run_id`, `deployment_name` (both required).
