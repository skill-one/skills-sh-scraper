# Bad Nested Fixture

## Scope

This file applies to the fixture repository except `packages/api/`, which has its own `AGENTS.md`.

## Context

- **Purpose**: Demonstrates duplicated nested guidance.

## Project Rules

- Add API routes under `packages/api/src/routes/` even though `packages/api/AGENTS.md` owns API-specific rules.
