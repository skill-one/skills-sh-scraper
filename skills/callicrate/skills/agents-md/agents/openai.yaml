interface:
  display_name: "AGENTS.md Author"
  short_description: "Create repo-grounded agent guidance"
  default_prompt: "Create or update this repository's AGENTS.md using verified project evidence, existing docs, command contracts, and current file topology. Keep guidance concise and repo-specific."
policy:
  allow_implicit_invocation: true
